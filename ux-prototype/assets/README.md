# UX Prototype Assets

This directory is reserved for local static assets used by the FoodFighter high-fidelity prototype.

## Asset Policy
- Pure CSS styling and inline SVG icons are used throughout the prototype to ensure 100% `file://` compatibility without external network dependencies.
- No copyrighted images or external font/icon CDNs are required.
